# Fonts

## Instalation

1. Unzip your fonts in `~/.local/share/fonts` (or `/usr/share/fonts`, for system wide install)
2. Run the command bellow on a terminal, in order to load the new font/s

   ```sh
   fc-cache -f -v
   ```
